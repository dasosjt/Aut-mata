COMPILER Ejemplo
CHARACTERS
letter =
KEYWORDS
if = "if"
while = "while"
TOKENS
id = letter{letter} EXCEPT KEYWORDS.
number = digit{digit}.
hexnumber = hexdigit{hexdigit}"(H)".
ident = {letter|digit}.
PRODUCTIONS
END Ejemplo
